import 'package:flutter/material.dart';

class Berries extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Berries'),
        backgroundColor: Theme.of(context).accentColor,
      ),
      body: Container(),
    );
  }
}
